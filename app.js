const form=document.querySelector('#search-form');const results=document.querySelector('#results');const username=document.querySelector('#username');const title=document.querySelector('#result-title');const sourceGrid=document.querySelector('#source-grid');const state=document.querySelector('#search-state');const connect=document.querySelector('#connect');const endpoint=document.querySelector('#endpoint');
const sources=['GitHub','GitLab','Reddit','X / Twitter','Instagram','YouTube','Twitch','Medium','Dev.to','Keybase','Mastodon','Telegram'];
form.addEventListener('submit',(event)=>{event.preventDefault();const value=username.value.trim();if(!value)return;title.textContent=`Résultats pour @${value}`;results.classList.remove('hidden');state.innerHTML='<span class="state-icon">✓</span><div><strong>Session préparée</strong><small>Le pseudo est prêt à être transmis à votre moteur Sherlock autorisé.</small></div>';sourceGrid.innerHTML=sources.map(name=>`<div class="source-chip">${name}<span>EN ATTENTE</span></div>`).join('');results.scrollIntoView({behavior:'smooth',block:'start'})});
connect.addEventListener('click',()=>{const value=endpoint.value.trim();if(!/^https:\/\//i.test(value)){endpoint.focus();endpoint.setCustomValidity('Utilisez un endpoint HTTPS autorisé.');endpoint.reportValidity();return}endpoint.setCustomValidity('');connect.textContent='Endpoint enregistré ✓';connect.style.background='var(--cyan)';});
